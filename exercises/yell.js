'use strict'

/*
 * Create a `yell` function that takes a string
 * and return the same string in upper case
 *
 */

 let yell = assert.toUpperCase();

//* Begin of tests
const assert = require('assert')

assert.fail('You must write your own tests')
// End of tests */
